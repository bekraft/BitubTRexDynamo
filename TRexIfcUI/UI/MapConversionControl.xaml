<UserControl x:Class="TRex.UI.MapConversionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TRex.UI"
             xmlns:mscore="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="180">
        
    <Grid MinHeight="50" MinWidth="150" Margin="3,3,3,3">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>

            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Label Grid.Row="0" 
               Grid.Column="0" 
               VerticalContentAlignment="Center"
               HorizontalContentAlignment="Left"
               Content="EPSG ID"
               Margin="2"/>
        
        <TextBox Grid.Row="0"
                 Grid.Column="1"
                 Margin="2"
                 TextWrapping="NoWrap"
                 Text="{Binding Path=CrsPrefs.Name}"/>
        
        <Label Grid.Row="1" 
               Grid.Column="0" 
               VerticalContentAlignment="Center"
               HorizontalContentAlignment="Left"
               Content="Geodetic datum"
               Margin="2"/>
        
        <TextBox Grid.Row="1"
                 Grid.Column="1"
                 Margin="2"
                 TextWrapping="NoWrap"
                 Text="{Binding Path=CrsPrefs.VerticalDatum}"/>
        
        <!--<ComboBox x:Name="CRSTransformComboBox" 
                  Grid.Row="0" 
                  Grid.Column="1"
                  VerticalAlignment="Center"
                  SelectedItem="{Binding Path=Transform}"
                  ItemsSource="{Binding Path=ItemsTransform}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Name, Mode=OneWay}" />
                    </StackPanel>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>-->

    </Grid>
</UserControl>
